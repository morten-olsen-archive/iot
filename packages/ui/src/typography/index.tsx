import styled from 'styled-components/native';

const Base = styled.Text``;

export const Display1 = styled(Base)`
  font-size: 48px;
`;

export const Body1 = styled(Base)``;

export const Paragraph = styled(Body1)``;
